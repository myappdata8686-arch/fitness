enum PhaseId {
  phase0RegulationRepair,
  phase1FoundationFatLoss,
  phase2MaintenanceRecess,
  phase3Recomposition,
  phase4ForeverAthletic,
}

enum DayColor { red, yellow, green }

enum StressLevel { low, medium, high }

enum EnergyLevel { low, medium, high }

enum WeekLabel {
  progressive,
  stable,
  maintenance,
  correction,
  adaptive,
}
